// OpenAI's Images API offers these three shapes only — there is no 4:5 or 9:16,
// so they are named by shape rather than by a ratio it cannot produce.
export type ImageShape = "square" | "portrait" | "landscape";
