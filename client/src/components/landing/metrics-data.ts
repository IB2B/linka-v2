export type Metric = {
  value: string;
  label: string;
  hint: string;
};

// Demo figures in the same order as `landing.metrics.items` in the message files.
export const METRIC_VALUES = ["184M", "+312%", "4 min", "97%"];
