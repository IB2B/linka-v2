import { Plus_Jakarta_Sans } from "next/font/google";

// Applied on the landing page wrapper only — the rest of the app stays on Inter
// via --font-sans in the root layout.
export const landingFont = Plus_Jakarta_Sans({ subsets: ["latin"] });
