// Indexes of features shipped recently enough to still be news — they get a
// "New" marker. Keep this short; a badge on half the list stops meaning much.
export const NEW_FEATURES: ReadonlySet<number> = new Set([3, 4]);
