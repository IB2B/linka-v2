import { Wand2, Image as ImageIcon, CalendarClock, BarChart3, Inbox, Radar, RefreshCw, Workflow } from "lucide-react";
import type { LucideIcon } from "lucide-react";

// Icons in the same order as `landing.features.items` in the message files.
export const FEATURE_ICONS: LucideIcon[] = [
  Wand2, ImageIcon, CalendarClock, Radar, Inbox, BarChart3, RefreshCw, Workflow,
];
