import { Rocket, Mic, Building2, LineChart } from "lucide-react";
import type { LucideIcon } from "lucide-react";

// Icons in the same order as `landing.useCases.items` in the message files.
export const USE_CASE_ICONS: LucideIcon[] = [Rocket, Mic, Building2, LineChart];
